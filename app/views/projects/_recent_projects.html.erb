<% get_recent_projects.each do |p| %>
  <li>
    <%= link_to ("#{image_tag 'standart_godzilla.png', size: '16x16'}" + " #{p.name}").html_safe, project_issues_path(p) %>
  </li>
<% end if get_recent_issues %>